HHVM Wordpress instance with Syslog configured for Nginx
===========================

This image doesn't actually contain a Wordpress installation. Rather, you will need to
do that yourself. You can do it when you create a data volume and install it into:
```/usr/share/nginx/www```.